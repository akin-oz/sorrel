---
description: Scaffold the next specs/NNN-*.md proposal (approved: no) from a one-line gap.
argument-hint: <short feature/gap description>
---

A feature gap was identified: **$ARGUMENTS**

Create the next specification proposal:

1. List `specs/` and pick the next free zero-padded `NNN`.
2. Delegate to the **spec-author** agent to write `specs/NNN-<slug>.md` for: $ARGUMENTS
3. Do NOT implement anything and do NOT set `approved: yes` — that is the human's call.
4. Report the path and the single decision the human must approve before work can start.
